# Python Examples

A collection of Python scripts and small projects.

## Structure

- `examples/` — small, standalone scripts and snippets
- `projects/` — larger, self-contained projects (each in its own subfolder)
